# snow-removal photos

Drop exactly 3 photos here for the "snow-removal" section on the Services page.
Recommended: 4:3 aspect ratio (e.g. 1200x900), compressed for web.

Name them anything; just update the matching service's `images` array
in `src/data/services.js` to point at your filenames.
